version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - APYFY_API_TOKEN=${APYFY_API_TOKEN}
      - GROQ_API_KEY=${GROQ_API_KEY}
    restart: always
